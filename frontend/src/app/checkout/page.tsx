"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { ArrowLeft, Check, CreditCard, LockKeyhole, MapPin, Truck } from "lucide-react";
import { FormEvent, useState } from "react";
import Container from "@/components/ui/Container";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";
import Offer from "@/components/Offer/Offer";
import { useCart } from "@/components/cart/CartProvider";
import { formatPrice } from "@/app/lib/shopCatalog";

type Delivery = "standard" | "express";
type Payment = "card" | "upi" | "cod";

export default function CheckoutPage() {
  const router = useRouter();
  const { items, subtotal, clearCart } = useCart();
  const [delivery, setDelivery] = useState<Delivery>("standard");
  const [payment, setPayment] = useState<Payment>("card");
  const [error, setError] = useState("");
  const shipping = delivery === "express" ? 199 : subtotal >= 999 ? 0 : 99;
  const total = subtotal + shipping;

  const placeOrder = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    if (items.length === 0) {
      setError("Your cart is empty. Add a product before checkout.");
      return;
    }

    const orderId = `VC-${Date.now().toString().slice(-8)}`;
    router.push(`/order-success?order=${orderId}`);
    window.setTimeout(clearCart, 0);
  };

  if (items.length === 0) {
    return <div className="min-h-screen bg-[#f7f8fa] text-slate-900"><Offer /><Navbar /><main><Container className="py-20"><div className="mx-auto max-w-lg rounded-3xl border border-dashed border-slate-300 bg-white px-6 py-16 text-center"><h1 className="text-2xl font-semibold">Nothing to checkout</h1><p className="mt-3 text-sm text-slate-500">Your cart is empty. Add products before starting checkout.</p><Link href="/shop" className="mt-6 inline-flex rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white hover:bg-orange-600">Continue shopping</Link></div></Container></main><Footer /></div>;
  }

  return <div className="min-h-screen bg-[#f7f8fa] text-slate-900"><Offer /><Navbar /><main><section className="border-b border-slate-200 bg-white"><Container><div className="py-10 lg:py-14"><Link href="/cart" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-orange-600"><ArrowLeft size={16} /> Back to cart</Link><h1 className="mt-8 text-4xl font-semibold tracking-tight text-slate-950">Checkout</h1><p className="mt-3 text-slate-500">Securely complete your VisionCart order.</p></div></Container></section><Container className="py-10 lg:py-14"><form onSubmit={placeOrder} className="grid gap-8 lg:grid-cols-[1fr_360px]"><div className="space-y-6"><section className="rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-orange-100 text-sm font-bold text-orange-600">1</span><div><h2 className="text-xl font-semibold">Contact information</h2><p className="mt-1 text-sm text-slate-500">We will send your order updates here.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Full name<input required name="name" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" placeholder="Your name" /></label><label className="text-sm font-medium text-slate-700">Email<input required type="email" name="email" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" placeholder="you@example.com" /></label><label className="text-sm font-medium text-slate-700 sm:col-span-2">Phone number<input required type="tel" name="phone" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" placeholder="+91 98765 43210" /></label></div></section><section className="rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-orange-100 text-sm font-bold text-orange-600">2</span><div><h2 className="text-xl font-semibold">Delivery address</h2><p className="mt-1 text-sm text-slate-500">Where should we deliver your order?</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700 sm:col-span-2">Address<input required name="address" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" placeholder="House number and street" /></label><label className="text-sm font-medium text-slate-700">City<input required name="city" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" placeholder="New Delhi" /></label><label className="text-sm font-medium text-slate-700">Postal code<input required pattern="[0-9]{5,6}" name="postal" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" placeholder="110001" /></label></div></section><section className="rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-orange-100 text-sm font-bold text-orange-600">3</span><div><h2 className="text-xl font-semibold">Delivery method</h2><p className="mt-1 text-sm text-slate-500">Choose when your order arrives.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><label className={`flex cursor-pointer items-center justify-between rounded-2xl border p-4 ${delivery === "standard" ? "border-orange-400 bg-orange-50" : "border-slate-200"}`}><span className="flex items-center gap-3"><Truck size={19} className="text-orange-600" /><span><strong className="block text-sm">Standard</strong><small className="text-slate-500">3-5 business days</small></span></span><input type="radio" name="delivery" checked={delivery === "standard"} onChange={() => setDelivery("standard")} /></label><label className={`flex cursor-pointer items-center justify-between rounded-2xl border p-4 ${delivery === "express" ? "border-orange-400 bg-orange-50" : "border-slate-200"}`}><span className="flex items-center gap-3"><Truck size={19} className="text-orange-600" /><span><strong className="block text-sm">Express</strong><small className="text-slate-500">1-2 business days</small></span></span><input type="radio" name="delivery" checked={delivery === "express"} onChange={() => setDelivery("express")} /></label></div></section><section className="rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-orange-100 text-sm font-bold text-orange-600">4</span><div><h2 className="text-xl font-semibold">Payment method</h2><p className="mt-1 text-sm text-slate-500">Demo checkout supports your preferred option.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{([['card', 'Card', CreditCard], ['upi', 'UPI', LockKeyhole], ['cod', 'Cash on delivery', MapPin]] as const).map(([value, label, Icon]) => <label key={value} className={`cursor-pointer rounded-2xl border p-4 ${payment === value ? "border-orange-400 bg-orange-50" : "border-slate-200"}`}><span className="flex items-center gap-2 text-sm font-semibold"><Icon size={17} className="text-orange-600" /> {label}</span><input className="mt-4" type="radio" name="payment" checked={payment === value} onChange={() => setPayment(value)} /></label>)}</div></section></div><aside className="h-fit rounded-3xl border border-slate-200 bg-white p-6 lg:sticky lg:top-28"><h2 className="text-xl font-semibold">Order summary</h2><div className="mt-6 space-y-4">{items.map(({ product, quantity }) => <div key={product.id} className="flex justify-between gap-4 text-sm"><span className="text-slate-500">{product.title} <strong className="text-slate-700">× {quantity}</strong></span><span className="shrink-0 font-semibold">{formatPrice(product.price * quantity)}</span></div>)}<div className="space-y-3 border-t border-slate-100 pt-4 text-sm"><div className="flex justify-between text-slate-500"><span>Subtotal</span><span>{formatPrice(subtotal)}</span></div><div className="flex justify-between text-slate-500"><span>Shipping</span><span>{shipping === 0 ? "Free" : formatPrice(shipping)}</span></div><div className="flex justify-between border-t border-slate-100 pt-3 text-base font-bold"><span>Total</span><span>{formatPrice(total)}</span></div></div></div>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</p>}<button type="submit" className="mt-6 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-orange-600 text-sm font-semibold text-white transition hover:bg-orange-500"><Check size={17} /> Place order</button><p className="mt-4 text-center text-xs text-slate-400">Your information is encrypted and secure.</p></aside></form></Container></main><Footer /></div>;
}
